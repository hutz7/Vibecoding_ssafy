# [보충 자료] 유닛 테스트 — 개발 입문자를 위한 이론

> 교안에는 없지만, 현업에서는 상식처럼 쓰이는 내용의 일부 입니다.


---

## 0. 들어가기 전에 — 왜 이 내용이 필요한가

여러분이 오늘 배운 건 이거였어요.

> "AI한테 테스트 코드 짜달라고 하면 짜준다."

맞습니다. 그런데 여기서 **결정적인 문제**가 하나 생겨요.

**AI가 짜준 테스트가 좋은 테스트인지, 쓰레기 테스트인지 여러분이 판단할 수 없다는 것.**

AI는 시키면 100개도 짜줍니다. 그런데 그중에 **아무 의미 없는 테스트가 80개**일 수도 있어요.
그걸 걸러내려면, **"좋은 테스트가 뭔지"** 를 알아야 합니다.

그래서 이 시간을 씁니다.

---

## 1. 테스트에는 층(層)이 있다 — 테스트 피라미드

실무에서 "테스트"라고 하면 사실 **여러 종류**를 뭉뚱그려 부르는 겁니다.
우리가 배운 유닛 테스트는 그중 **맨 아래 한 칸**이에요.

```
           /\
          /  \        E2E 테스트  (End to End, 처음부터 끝까지)
         /____\       "사용자처럼 진짜 클릭해본다"
        /      \      느림 😴 / 비쌈 💸 / 적게
       /        \
      /  통합    \    통합 테스트 (Integration)
     /   테스트    \   "부품 여러 개를 붙여서 같이 돌려본다"
    /______________\
   /                \
  /   유닛 테스트     \  ← 오늘 우리가 배운 것
 /   (Unit Test)      \  "부품 하나만 딱 떼서 검사한다"
/______________________\ 빠름 ⚡ / 쌈 💰 / 많이
```

### 자동차로 비유하면

| 층 | 자동차 비유 | 개발 예시 |
|---|---|---|
| **유닛 테스트** | **엔진만** 떼서 시동 걸어보기 | `더하기(2,3)`이 5를 뱉나? |
| **통합 테스트** | 엔진 + 변속기 + 바퀴를 **연결해서** 굴려보기 | 로그인 버튼 → 서버 → DB 저장까지 잘 되나? |
| **E2E 테스트** | 사람이 **차에 타고 도로를 달려보기** | 사용자가 회원가입→로그인→글쓰기 전 과정이 되나? |

### 왜 피라미드 모양일까? (아래가 넓다)

**유닛 테스트는 싸고 빠르니까 많이 짜고, E2E는 비싸고 느리니까 적게 짜라**는 뜻입니다.

- 유닛 테스트 100개 → **1초**면 다 돌아요
- E2E 테스트 100개 → 브라우저 켜고 클릭하고… **30분** 걸립니다

그리고 결정적으로 —

> **유닛 테스트가 깨지면 "어디가" 깨졌는지 바로 압니다.** (`더하기` 함수요!)
> **E2E가 깨지면 "뭔가" 안 된다는 것만 압니다.** (어디가 문제인지는 또 찾아야 해요)

**💡 입문자가 가져갈 결론:**
"테스트 = 유닛 테스트"가 아닙니다. 하지만 **유닛 테스트부터 시작하는 게 맞습니다.**
가장 싸고, 가장 빠르고, 가장 정확하게 범인을 잡아주니까요.

---

## 2. 좋은 테스트의 뼈대 — Given / When / Then

실무 테스트 코드는 **거의 다 똑같은 모양**입니다. 세 덩어리예요.

```javascript
test('3에서 5를 빼면 음수라서 0이 나와야 한다', () => {
  // 1) Given (준비) — "이런 상황이 주어졌을 때"
  const 첫번째 = 3;
  const 두번째 = 5;

  // 2) When (실행) — "이걸 실행하면"
  const 결과 = 빼기(첫번째, 두번째);

  // 3) Then (검증) — "이렇게 나와야 한다"
  expect(결과).toBe(0);
});
```

이걸 **Given-When-Then** 이라고 부릅니다. (다른 데선 **AAA 패턴** = Arrange/Act/Assert 라고도 해요. 같은 얘기입니다.)

### 왜 굳이 이렇게 나눠 쓸까?

**읽는 사람을 위해서입니다.**

테스트 코드는 **한 번 쓰고 끝이 아니라, 6개월 뒤에 남이 읽습니다.**
그때 이 세 덩어리로 나뉘어 있으면, 3초 만에 이해돼요.

> "아, **3이랑 5가 있을 때**(Given), **빼기를 하면**(When), **0이 나와야 하는구나**(Then)."

**💡 AI에게 시킬 때 이렇게 말하세요:**
```
테스트 코드는 Given-When-Then 구조로, 각 단계에 주석을 달아서 작성해줘.
```

---

## 3. 테스트 이름이 곧 문서다

초보자가 제일 많이 하는 실수예요.

```javascript
test('테스트1', ...)          // ❌ 이게 뭘 테스트하는지 아무도 모름
test('더하기 테스트', ...)     // ❌ 더하기의 뭘? 정상? 에러?
test('add test', ...)         // ❌ 마찬가지
```

좋은 이름은 **문장**입니다. 이 공식으로 쓰세요.

> ## **"[어떤 상황]에서 [무엇을] 하면 [어떻게 된다]"**

```javascript
test('빼기 결과가 음수이면 0으로 바뀌어야 한다', ...)            // ✅
test('잔액보다 큰 금액을 쓰면 "잔액이 부족합니다"가 출력된다', ...) // ✅
test('숫자가 아닌 값을 입력하면 에러 메시지가 나온다', ...)        // ✅
```

---

## 4. 그래서 뭘 테스트해야 하죠? — 3종 세트

"테스트 짜라"고 하면 다들 **정상 케이스만** 짭니다. 2+3=5 같은 거요.
그런데 **실무 버그는 99% 정상 케이스 밖에서 터집니다.**

항상 이 **3종 세트**를 챙기세요.

| 종류 | 뜻 | 계산기 예시 |
|---|---|---|
| ☀️ **정상 (Happy Path)** | 잘 될 때 | 2 + 3 = 5 |
| ⚠️ **경계 (Boundary)** | **딱 걸치는 값** | 5 - 5 = 0 (음수 직전!), 잔액 = 결제금액 |
| 💥 **예외 (Edge/Error)** | 이상한 걸 넣을 때 | 글자 입력, 빈 칸, 0으로 나누기 |

### ⚠️ 경계값이 왜 중요한지 — 실제 사고 이야기

이런 코드가 있다고 합시다.

```javascript
if (잔액 > 결제금액) {   // "보다 크면" 결제 가능
   결제하기();
}
```

**잔액이 5000원, 결제금액도 정확히 5000원이면?**

`5000 > 5000` → **거짓!** → **결제가 안 됩니다.**

(잠깐 생각해보세요) 잔액이 딱 맞는데 결제가 안 되는 거예요.
`>` 를 `>=` 로 써야 했는데, 등호 하나 빠뜨린 거죠.

**실무 버그의 절반은 이런 "하나 차이"에서 나옵니다.**
이걸 개발자들은 **'울타리 기둥 오류(Off-by-one error)'** 라고 불러요.
울타리 10칸을 만들려면 기둥은 11개 필요하죠. 딱 하나씩 어긋나는 실수요.

> **그래서 경계값 테스트는 "딱 그 지점, 하나 앞, 하나 뒤" 3개를 짭니다.**
> 잔액 4999 / **5000** / 5001 → 이렇게요.

**💡 AI에게 시킬 때:**
```
정상 케이스뿐 아니라 경계값(딱 0이 되는 값, 최댓값)과
예외 케이스(빈 값, 글자 입력, 0으로 나누기)에 대한 테스트도 함께 만들어줘.
```

---

## 5. 좋은 테스트의 5가지 조건 (F.I.R.S.T)

실무에서 통용되는 원칙입니다. 5개 다 외울 필욘 없고, **핵심 2개만** 기억하세요.

| 원칙 | 뜻 | 쉽게 말하면 |
|---|---|---|
| **F**ast | 빨라야 한다 | 느리면 아무도 안 돌립니다. 안 돌리면 없는 것과 같아요 |
| ⭐ **I**ndependent | **독립적이어야 한다** | **테스트끼리 서로 영향을 주면 안 됨** |
| ⭐ **R**epeatable | **반복 가능해야 한다** | **몇 번을 돌려도, 누가 돌려도 같은 결과** |
| **S**elf-validating | 스스로 판단해야 한다 | 통과/실패가 자동으로 나와야지, 사람이 눈으로 보면 안 됨 |
| **T**imely | 제때 짜야 한다 | 나중에 짜야지~ 하면 영원히 안 짭니다 |

### ⭐ Independent (독립) — 제일 많이 어기는 것

**나쁜 예:**
```javascript
let 잔액 = 1000;                       // 공용 변수 😱

test('500원 쓰면 500원 남는다', () => {
  잔액 = 잔액 - 500;                    // 잔액이 500이 됨
  expect(잔액).toBe(500);               // ✅ 통과
});

test('300원 쓰면 700원 남는다', () => {
  잔액 = 잔액 - 300;                    // 앞 테스트 때문에 이미 500이었음!
  expect(잔액).toBe(700);               // ❌ 실패! (실제로는 200)
});
```

**뭐가 문제죠?** 앞 테스트가 잔액을 건드려놨어요.
그래서 **두 번째 테스트는 자기 잘못이 아닌데 실패**합니다.

더 무서운 건, **테스트 실행 순서가 바뀌면 결과도 바뀐다**는 거예요.
이런 걸 **"플레이키 테스트(Flaky Test)"** — 변덕스러운 테스트라고 부릅니다.
실무에서 **가장 미움받는 존재**예요. 사람들이 "아 얘 또 저래" 하면서 **테스트를 무시하기 시작**하거든요.

> **규칙: 모든 테스트는 자기가 쓸 재료를 자기가 새로 준비한다.**
> 남이 쓰던 걸 물려받지 않는다.

**고친 예:**
```javascript
test('500원 쓰면 500원 남는다', () => {
  let 잔액 = 1000;                      // ✅ 매번 새로 준비
  잔액 = 잔액 - 500;
  expect(잔액).toBe(500);
});

test('300원 쓰면 700원 남는다', () => {
  let 잔액 = 1000;                      // ✅ 얘도 새로 준비
  잔액 = 잔액 - 300;
  expect(잔액).toBe(700);               // ✅ 이제 통과
});
```

### ⭐ Repeatable (반복 가능) — 숨은 지뢰

이런 코드는 테스트하기가 **불가능**합니다.

```javascript
function 할인가격(원가) {
  if (오늘이_토요일인가()) {     // 😱 오늘이 언제냐에 따라 결과가 달라짐
    return 원가 * 0.5;
  }
  return 원가;
}
```

토요일에 테스트하면 통과하고, 일요일에 돌리면 실패해요. **이건 테스트가 아닙니다.**

**테스트를 망치는 3대 요소:**
- 🕐 **현재 시각** (`오늘 날짜`, `지금 몇 시`)
- 🎲 **랜덤** (`랜덤 숫자`)
- 🌐 **외부 서버** (진짜 API 호출, 진짜 DB)

이것들이 코드 안에 있으면 **결과가 매번 바뀌어서** 테스트를 못 해요.
그럼 어떻게 하냐고요? 다음 항목입니다.

---

## 6. 가짜(Mock)를 쓴다 — 대역 배우

영화 촬영에서 위험한 장면은 **스턴트 대역**이 하죠. 주연 배우가 진짜로 불타는 차에 타진 않잖아요.

테스트도 똑같습니다.

### 문제 상황

결제 기능을 테스트하고 싶어요. 그런데 이 코드는 **진짜 카드사 서버**를 호출합니다.

```javascript
function 결제하기(금액) {
  const 결과 = 카드사서버.결제요청(금액);   // 😱 진짜 돈이 나감!!
  ...
}
```

이걸 테스트로 100번 돌리면? **진짜 결제가 100번 됩니다.** 😱

### 해결: 가짜를 만든다

```javascript
// 카드사 서버인 척하는 '가짜'. 항상 "성공"이라고만 대답한다.
const 가짜카드사 = {
  결제요청: (금액) => { return { 성공: true }; }
};
```

이제 **진짜 돈은 안 나가면서**, 결제 로직이 잘 돌아가는지 검사할 수 있어요.
이런 가짜를 **Mock(목)** 또는 **Stub(스텁)** 이라고 부릅니다.
(둘의 차이는 지금 몰라도 됩니다. 실무 가면 자연히 배웁니다. **"대역 배우"** 로만 기억하세요.)

### 이걸 왜 입문자에게 말하냐면

여러분이 나중에 **API 붙이고, 로그인 붙이고, DB 붙이면** 반드시 벽에 부딪힙니다.

> "어? 이거 테스트하려면 진짜 서버가 켜져 있어야 되네?"
> "어? 테스트 돌릴 때마다 DB에 쓰레기 데이터가 쌓이네?"

**그때 이 단어를 떠올리세요. "아, Mock을 써야 하는구나."**
그리고 AI한테 이렇게 물어보면 됩니다.

```
이 함수는 외부 API를 호출해서 테스트하기 어려워.
API 호출 부분을 Mock으로 대체해서 테스트 코드를 짜줘.
```

> **핵심 교훈: 테스트하기 어려운 코드 = 잘못 설계된 코드일 확률이 높다.**
> 실무에서는 "테스트가 안 짜지네?" 가 **설계를 다시 보라는 신호**입니다.

---

## 📌 정리 

1. **테스트에는 층이 있다.** 유닛(부품) → 통합(조립) → E2E(주행). **유닛부터 시작한다.**
2. **테스트는 Given-When-Then** 세 덩어리로 쓴다. 이름은 **완전한 문장**으로 짓는다.
3. **정상만 테스트하지 마라.** 버그는 **경계값**과 **예외**에서 터진다. (5 > 5 는 거짓이다!)
4. **테스트끼리 서로 간섭하면 안 된다.** 시각·랜덤·외부서버가 끼면 **Mock(대역)** 을 쓴다.

---
